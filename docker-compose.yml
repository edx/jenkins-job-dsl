version: "3"
services:
  jenkins:
    image: jenkins:alpine
    container_name: tools_jenkins
    volumes:
        - jenkins:/var/jenkins_home
    ports:
      - "127.0.0.1:8080:8080"

volumes:
  jenkins:
