# incomplete-secret.yml
# This is a valid yml file, however, it does not contain all of the necessary fields for use in the dsl script "platform/jobs/edxPlatformTestSubset.groovy". This can be used to test that the dsl script handles the case of incorrect secret data correctly.
publicJobConfig:
    open : true
    jobName : edx-platform-test-subset
    credential : n/a
    cloneReference : public-clone/.git
privateJobConfig:
    open : false
    jobName : edx-platform-test-subset_2
    credential : password
    cloneReference : private-clone/.git
